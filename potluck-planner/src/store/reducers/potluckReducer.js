// import { SUCCESS, FAIL, LOADING} from '../actions'

const initialState = {
    potlucks: [],
    loading: false,
    error: ''
}

export const potluckReducer = (state = initialState, action) => {
    switch(action.type) {
        default:
            return state
    }
}